# Python Theme Switcher

## Basic Usage
`python main.py <theme-name>.json`

This is just a small project to force myself to try out Python.
For now, I'm basically just calling bash commands via python, but I'll clean it up later.
